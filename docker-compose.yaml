version: "1"

services:
  gunicorn:
    build: backend
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    network_mode: "host"
  react:
    build: frontend
    restart: always
    ports:
      - "80:3000"
    network_mode: "host"
